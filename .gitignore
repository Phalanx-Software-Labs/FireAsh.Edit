# This repository only tracks the cheat-engine APK (and README + this file).
# Everything else stays on your machine for development but is not pushed.
/*
!/.gitignore
!/README.md
!/FireAshCheatEngine-debug.apk

# This repository only tracks the installer APK (and README + this file).
# Everything else stays on your machine for development but is not pushed.
/*
!/.gitignore
!/README.md
!/FireAshModInstaller-debug.apk
